apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: api-creds
spec:
  secretStoreRef:
    name: aws-ca-central-1
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: dev/mentum/api-creds