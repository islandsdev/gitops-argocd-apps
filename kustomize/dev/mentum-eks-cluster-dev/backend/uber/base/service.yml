---
# Source: default/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: uber
  labels:
    app.kubernetes.io/tier: backend
    app.kubernetes.io/app: uber
spec:
  type: ClusterIP
  ports:
    - name: uber-port
      targetPort: 8084
      protocol: TCP
      port: 80
  selector:
    # app.kubernetes.io/name: uber
    # app.kubernetes.io/instance: uber
    app.kubernetes.io/app: uber