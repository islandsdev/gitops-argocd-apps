---
# Source: default/templates/pdb.yaml
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  labels:
    app: socket
    chart: default-0.1.0
    environment: ""
    heritage: Helm
    release: socket
  name: socket
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: socket
      release: socket