---
# Source: default/templates/pdb.yaml
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  labels:
    app: api
    environment: dev
    release: api
  name: api
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: api
      release: api